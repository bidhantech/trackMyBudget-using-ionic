import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'earnings-and-spendings-diary',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
